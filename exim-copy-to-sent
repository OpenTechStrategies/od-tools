if "${if match {${lookup{$sender_address}lsearch{/etc/exim4/virtual.users}}}{@localhost}{yes}{}}" is "yes" then
	if "${if def:h_X-Spam-Status {def}{undef}}" is "undef" then
		unseen save $home/Maildir/.Sent/
		unseen pipe "/var/www/tools/mark-as-read.pl \"$recipients\""
	endif
endif
